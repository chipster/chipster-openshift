apiVersion: v1
kind: Service
metadata:
  name: {{ include "chipster.fullname" . }}
  labels:
    {{- include "chipster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chipster.selectorLabels" . | nindent 4 }}
