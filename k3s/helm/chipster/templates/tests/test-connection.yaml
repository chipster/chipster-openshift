apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chipster.fullname" . }}-test-connection"
  labels:
{{ include "chipster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chipster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
