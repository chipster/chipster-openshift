---
apiVersion: v1
kind: DeploymentConfig
metadata:
  name: auth-h2
  labels:
    app: auth
spec:
  template:
    metadata:
      labels:
        app: auth-h2
    spec:
      containers:
      - name: auth-h2
        image: docker-registry.default.svc:5000/{{project}}/h2
        ports:                
        - name: h2-server
          containerPort: 1521
        - name: h2-console
          containerPort: 8081
        volumeMounts:
        - mountPath: /opt/h2-data
          name: h2-data
        readinessProbe:
          tcpSocket:
            port: 1521
      volumes:
      - name: h2-data
        persistentVolumeClaim:
          claimName: auth-h2-data
  replicas: 1
          