---
spec:
  template:    
    spec:
      containers:
      - name: comp-mylly
        resources:
          limits:
            cpu: 1900m
            memory: 7900Mi
          requests:
            # about half of limits to kill comps before more crucial services if the host runs out of memory
            cpu: 1000m
            memory: 4000Mi
        volumeMounts:
        - mountPath: /opt/chipster/comp/jobs-data
          name: jobs-data
        - mountPath: /appl
          name: tools-bin
      volumes:
      - name: jobs-data
        persistentVolumeClaim:
          claimName: comp-jobs-data-mylly
      - name: tools-bin
        persistentVolumeClaim:
          claimName: tools-bin-mylly
