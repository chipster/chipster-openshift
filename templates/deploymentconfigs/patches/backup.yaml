---
spec:
  template:    
    spec:
      containers:
      - name: backup
        volumeMounts:
        - mountPath: /opt/chipster-web-server/db-backups
          name: db-backups
      volumes:
      - name: db-backups
        persistentVolumeClaim:
          claimName: db-backups
