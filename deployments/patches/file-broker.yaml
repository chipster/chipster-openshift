---
spec:
  template:    
    spec:
      containers:
      - name: file-broker
        volumeMounts:
        - mountPath: /opt/chipster-web-server/storage
          name: storage
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: file-broker-storage
